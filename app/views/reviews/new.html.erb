<div class="container">
  <div class="row">
    <div class="col-xs-4">
      <%= simple_form_for([@product, @review]) do |f| %>
        <%= f.input :content %>
        <%= f.input :rating, collection: (1..5) %>
        <%= f.submit "Submit review" %>
      <% end %>
    </div>
  </div>
</div>
