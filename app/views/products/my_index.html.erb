
<% @products.each do |product|  %>
<div>
  <h1> <%= product.name %> </h1>
  <h2>  <%= product.price %> </h2>
 <p> <%= link_to "Delete", product_path(product.id), method: :delete %> </p>
</div>
<% end %>
